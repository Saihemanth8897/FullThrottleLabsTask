# FullThrottleLabsTask

1) Check you have Angular Environment in your local 
2) clone the Project 
3) set the path to that folder and enter command in CMD "npm install"
4) run Project 'ng serve'
5) port number "localhost:4200"
